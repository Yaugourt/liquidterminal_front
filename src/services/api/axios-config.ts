// Redirection stub — the implementation lives in `./http/axios-config`.
// Kept here so external imports `@/services/api/axios-config` continue to work
// after the internal re-organization into http/cache/auth/validation subfolders.
export * from './http/axios-config';
